import RxSwift

class ArticlesViewModel: ViewModel {

//    private let disposeBag: DisposeBag = DisposeBag()
//    private let pricingService: PricingService!
//    private(set) var dataSource: TableViewDataSource<FlightResource, FlightCell> =
//        TableViewDataSource<FlightResource, FlightCell>()
//    private(set) var beginRefreshing: PublishSubject<Bool> = PublishSubject<Bool>()
//    private(set) var reloadTableView: PublishSubject<Void> = PublishSubject<Void>()
//    private(set) var publishFlightDates: PublishSubject<String> = PublishSubject<String>()

    required init(componentCreatable: ComponentCreatable) {
      //  pricingService = componentCreatable.create(with: componentCreatable)
    }

}
